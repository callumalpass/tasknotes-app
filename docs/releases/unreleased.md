# Unreleased

## Fixes

- Fixed reminder date, time, and offset edits failing to save with a misleading provider-unavailable error.
- Fixed removing the last reminder failing with an array validation error, including collections with a custom reminder field name.
